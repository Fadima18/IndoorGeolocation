[tox]
envlist = py39
skipdist = True

[testenv]
deps = 
    pytest
    coverage
commands =
    coverage run -m pytest
    coverage xml

[coverage:run]
relative_file = True
source = Geolocation/
branch = True